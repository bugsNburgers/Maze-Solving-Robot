# Donkey!Qdoc — Autonomous Maze Solving Turtlebot3 Simulation (ROS 2 Humble + Gazebo Classic)

Date: 2026-04-12
Workspace: `/home/prateek/Maze`
OS assumed: Ubuntu 22.04 (ROS 2 Humble)

This document has two goals:
1) **Explain the entire project in plain English** (so a total beginner can explain it back).
2) **Explain the same thing technically** (so you can debug it, modify it, and answer detailed questions).

It also includes a **timeline of what happened in this chat** (what broke, why, and how it was fixed), plus **run commands** and **troubleshooting**.

---

## 0) One‑sentence summary
This project launches a Turtlebot3 Waffle robot in a Gazebo Classic maze world, runs the Nav2 stack to localize/plan on a pre-made map, and uses a Python script to set an initial pose + send a goal so the robot drives through the maze.

---

## 1) What’s in this workspace (folder map)
At the top level:
- `README.md` — human instructions.
- `src/autonomous_tb3/` — the ROS 2 package you build and run.
- `build/`, `install/`, `log/` — generated by `colcon build`.

Inside `src/autonomous_tb3/`:
- `package.xml` — package metadata + declared dependencies.
- `CMakeLists.txt` — build/install rules (even though the code is Python).
- `launch/` — ROS 2 launch files (bring up Gazebo, RViz, Nav2, etc.).
- `config/` — maps, Nav2 parameters, RViz config, Cartographer config.
- `script/` — Python nodes (ROS 2 executables).
- `worlds/` — Gazebo world model(s) (SDF + model.config).

---

## 2) Plain English: what happens when you “run the project”
Think of this like a video game + autopilot:

1) **Gazebo Classic opens**
   - Gazebo is the simulator window.
   - The maze world is placed into an empty Gazebo world.
   - A Turtlebot3 robot model is spawned at a chosen starting position.

2) **RViz2 opens**
   - RViz is the visualization window.
   - It shows a *2D map*, robot pose estimate, laser scans, and Nav2 overlays.

3) **Nav2 stack starts**
   - Nav2 is the navigation brain.
   - It needs:
     - a map (a `.yaml` that references a `.pgm` image),
     - the robot’s sensors (laser scan),
     - transforms between frames (`map`, `odom`, `base_link`/`base_footprint`),
     - and an initial pose guess.

4) **maze_solver.py is run by you** (in a second terminal)
   - This script:
     - tells Nav2 “the robot starts here on the map” (initial pose),
     - then tells Nav2 “go to this goal location”.
   - Nav2 then publishes velocity commands (`/cmd_vel`),
     and the simulated robot moves.

---

## 3) Technical architecture (ROS 2 pieces and data flow)
### 3.1 Core external systems
- **Gazebo Classic**: physics simulation, robot model, world model.
- **gazebo_ros**: bridge layer for Gazebo <-> ROS 2 (spawn services, plugins).
- **turtlebot3_gazebo**: Turtlebot3 simulation assets + spawn launch files.
- **Nav2 (nav2_bringup)**: localization (AMCL), planning, control, recovery.
- **RViz2**: visualization.
- Optional mapping systems (present but disabled by default in the main launch):
  - **slam_toolbox** (recommended) or
  - **cartographer_ros** (configured via Lua).

### 3.2 Important ROS concepts used
- **Nodes**: running programs in ROS.
- **Topics**: streaming data channels (e.g., `/scan`, `/cmd_vel`).
- **Services**: request/response calls (e.g., Gazebo `/spawn_entity`).
- **Frames & TF**: coordinate transforms (`map`, `odom`, `base_link`, etc.).
- **use_sim_time**: simulation time comes from `/clock` instead of wall time.

### 3.3 Key topics you should expect
From earlier runtime inspection, typical topics include:
- `/cmd_vel` — velocity commands (Nav2 -> robot).
- `/scan` — LIDAR scans.
- `/odom` — odometry from simulated robot.
- `/tf`, `/tf_static` — transforms.
- `/amcl_pose` — pose estimate from AMCL.
- `/clock` — simulation time.

---

## 4) How to run (correct, repeatable steps)
### 4.1 Install dependencies (Ubuntu 22.04 / ROS 2 Humble)
The repository README lists the concept dependencies (Gazebo Classic, turtlebot3_gazebo, slam_toolbox, Nav2).

In this environment, the crucial missing items were:
- `python3-colcon-ros` (colcon plugin that lets `colcon` detect ROS packages via `package.xml`)
- and various ROS packages (`gazebo_ros_pkgs`, `nav2_bringup`, etc.).

### 4.2 Build
From workspace root:
```bash
cd /home/prateek/Maze
source /opt/ros/humble/setup.bash
colcon build --symlink-install
```

### 4.3 Run (two terminals)
Terminal 1:
```bash
cd /home/prateek/Maze
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch autonomous_tb3 tb3_maze_navigation.launch.py
```

Terminal 2 (only after Terminal 1 is running and Gazebo/RViz are up):
```bash
cd /home/prateek/Maze
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run autonomous_tb3 maze_solver.py
```

If it works, `maze_solver.py` will print something like:
- “Nav2 is ready for use!”
- “Navigating to goal …”
- then ETA countdown messages.

---

## 5) Timeline of what happened in this chat (what broke, why, fix)
This section is a factual log of events that occurred while setting the project up.

### 5.1 Initial launch failure: package not found
- Symptom:
  - `ros2 launch autonomous_tb3 tb3_maze_navigation.launch.py` failed with “Package 'autonomous_tb3' not found”.
- Root cause:
  - The workspace had not been built *in a way that made the package discoverable* in the current environment.
  - Also, `colcon build` was finishing with “0 packages finished”, which strongly indicates **colcon could not detect ROS packages**.

### 5.2 Colcon shows 0 packages: missing ROS discovery plugin
- Symptom:
  - `colcon build` and `colcon list` reported 0 packages.
- Root cause:
  - `colcon` was installed, but the ROS package discovery extension was missing.
- Fix:
  - Installed `python3-colcon-ros` (and its dependency `python3-colcon-cmake`).
  - After that, `colcon list --base-paths src` correctly showed:
    - `autonomous_tb3  (ros.ament_cmake)`

### 5.3 Launch failure: entity_spawner executable not found
- Symptom:
  - Launch error: executable `entity_spawner.py` not found in install libexec.
- Root cause:
  - CMake `install(PROGRAMS ...)` installs Python scripts as executables, but they must be **executable** on disk.
- Fix:
  - `chmod +x` on the Python scripts and rebuild.

### 5.4 Runtime conflict: Gazebo / entity already exists
- Symptom:
  - `gzserver` crashed and `spawn_entity` said “Entity [waffle] already exists”.
- Root cause:
  - stale Gazebo processes from previous runs.
- Fix:
  - kill stale `gzserver`, `gzclient`, `rviz2` processes and relaunch cleanly.

### 5.5 Robot not moving: AMCL / Nav2 activation issues + interrupt
There were two different failure modes:

1) **Earlier**: AMCL rejected initialpose as malformed and TF frames were missing.
   - Fix: updated `maze_solver.py` to publish a proper normalized quaternion and set `use_sim_time` for the navigator node.

2) **Later**: solver printed `amcl/get_state service not available`.
   - Root cause: Nav2 stack wasn’t fully active / got interrupted.
   - Confirmed by launch logs showing “user interrupted with ctrl-c (SIGINT)”.

### 5.6 Confirmed working
A clean run produced solver output:
- “Nav2 is ready for use!”
- “Navigating to goal …”
- ETA countdown decreasing.
That indicates Nav2 accepted initial pose, planned, and started commanding motion.

---

## 6) Package: `autonomous_tb3` (build + runtime overview)
This is an **ament_cmake** package that installs Python scripts as executables.

### 6.1 `package.xml`
File: `src/autonomous_tb3/package.xml`
Purpose:
- Defines package name `autonomous_tb3`.
- Declares build tool dependencies:
  - `ament_cmake`
  - `ament_cmake_python`
- Declares runtime dependency:
  - `rclpy`
Notes:
- Some runtime deps used by code (`nav_msgs`, `gazebo_msgs`, `geometry_msgs`, `nav2_simple_commander`) are not declared here; they are provided by ROS desktop installs in practice.

### 6.2 `CMakeLists.txt`
File: `src/autonomous_tb3/CMakeLists.txt`
Purpose:
- Finds build dependencies (ament + python support).
- Registers Python package folder `script` using `ament_python_install_package(script)`.
- Installs Python executables into `install/autonomous_tb3/lib/autonomous_tb3/`:
  - `occupancy_grid_pub.py`
  - `entity_spawner.py`
  - `maze_solver.py`
- Installs directories into share:
  - `config/`, `launch/`, `worlds/`

Practical implication:
- ROS will let you run:
  - `ros2 run autonomous_tb3 maze_solver.py`
  - `ros2 run autonomous_tb3 entity_spawner.py`
  - etc.

---

## 7) Launch files (what starts what)
Launch files are the “orchestra conductor” of ROS: they start multiple nodes and configure them.

### 7.1 `tb3_maze_navigation.launch.py` (main entry)
File: `src/autonomous_tb3/launch/tb3_maze_navigation.launch.py`
Goal:
- Start Gazebo server + client.
- Start robot state publisher for Turtlebot3.
- Spawn Turtlebot3 robot.
- Spawn the maze world model (custom SDF) via our own spawner node.
- Start RViz with the project config.
- Start Nav2 bringup with a provided map + params.

Main steps inside `generate_launch_description()`:
1) Compute paths:
   - Turtlebot3 Gazebo launch dir
   - Gazebo ROS package share
   - Maze SDF
   - Map YAML (`maze_map.yaml`)
   - Nav2 parameters YAML (`tb3_nav_params.yaml`)
   - RViz config (`tb3_nav.rviz`)
2) Set environment variable:
   - `TURTLEBOT3_MODEL=waffle`
3) Include Gazebo:
   - `gazebo_ros/gzserver.launch.py`
   - `gazebo_ros/gzclient.launch.py`
4) Include turtlebot3_gazebo components:
   - `robot_state_publisher.launch.py`
   - `spawn_turtlebot3.launch.py` with `x_pose`, `y_pose`.
5) Start custom maze spawner node:
   - package `autonomous_tb3`, executable `entity_spawner.py`, arguments = `[maze_path, model_name, x, y]`
6) Start RViz2 node with config.
7) Start Nav2 bringup:
   - `nav2_bringup/bringup_launch.py` with `map` and `params_file`.

Mapping options:
- `slam_toolbox` and `cartographer` includes exist but are commented out.

### 7.2 `navigation.launch.py` (alternative “world + nav”)
File: `src/autonomous_tb3/launch/navigation.launch.py`
Goal:
- Launch the default Turtlebot3 Gazebo world.
- Bring up Nav2 with map + params.
- Launch RViz.

This is not used by the main entry launch in this workspace.

### 7.3 `mapping.launch.py` (Cartographer mapping)
File: `src/autonomous_tb3/launch/mapping.launch.py`
Goal:
- Start `cartographer_node` and `cartographer_occupancy_grid_node` using `turtlebot3_cartographer.lua`.

This is also not enabled in the main launch by default.

---

## 8) Scripts (ROS nodes) — function-by-function
### 8.1 `entity_spawner.py`
File: `src/autonomous_tb3/script/entity_spawner.py`
Role:
- Spawns a Gazebo entity (here: the maze world model) by calling the Gazebo ROS service `/spawn_entity`.

Imports:
- `gazebo_msgs.srv.SpawnEntity` — service type.
- `rclpy` — ROS 2 Python client library.

Functions:
- `main()`
  - Reads argv: `[sdf_path, model_name, x, y]`.
  - Initializes ROS with `rclpy.init()`.
  - Creates a node named `spawning_node`.
  - Creates a service client to `/spawn_entity`.
  - Waits for service readiness.
  - Fills a `SpawnEntity.Request()`:
    - `request.name` = model name.
    - `request.xml` = contents of SDF file.
    - `request.initial_pose.position.(x,y)` = spawn coordinates.
  - Calls the service asynchronously and spins until complete.
  - Prints the response.
  - Shuts down cleanly.

Behavior:
- If Gazebo isn’t running or the `/spawn_entity` service isn’t available, it will wait.
- If you spawn a model with the same name twice, Gazebo may reject it.

### 8.2 `maze_solver.py`
File: `src/autonomous_tb3/script/maze_solver.py`
Role:
- Uses Nav2 Simple Commander to:
  1) set the robot initial pose,
  2) wait for Nav2 to be active,
  3) send a goal pose,
  4) print ETA feedback.

Imports:
- `PoseStamped` — pose message.
- `BasicNavigator`, `TaskResult` — Nav2 Simple Commander API.
- `rclpy`, `Duration`, `Parameter`.
- `sin`, `cos`.

Functions:
- `main()`
  1) `rclpy.init()`
  2) Create `navigator = BasicNavigator()`
  3) Set sim time parameter on navigator node:
     - `use_sim_time = True`
  4) Build `initial_pose`:
     - `frame_id = 'map'`
     - position set to a hard-coded starting coordinate.
     - quaternion set using `sin/cos` (normalized) for yaw = 0.2 rad.
     - `navigator.setInitialPose(initial_pose)`
  5) `navigator.waitUntilNav2Active()`
  6) Build `goal_pose`:
     - `frame_id = 'map'`
     - position set to a hard-coded goal coordinate.
     - quaternion set similarly.
     - `navigator.goToPose(goal_pose)`
  7) Loop until task complete:
     - prints ETA every 5 feedback cycles.
  8) Print final result based on `TaskResult`.
  9) Shutdown nav2 lifecycle.

Important behavior / gotchas:
- If Nav2 isn’t running, it will wait.
- If AMCL never receives/accepts initial pose, Nav2 can’t localize and won’t drive.
- If your launch is killed (SIGINT), `amcl/get_state` will disappear and solver will wait forever.

### 8.3 `occupancy_grid_pub.py`
File: `src/autonomous_tb3/script/occupancy_grid_pub.py`
Role:
- Example publisher of `nav_msgs/OccupancyGrid` on topic `occupancy_grid_map`.

Classes:
- `Occupancy_Grid_Publisher(Node)`
  - `__init__()`:
    - creates publisher for OccupancyGrid.
    - creates timer at 0.5s.
  - `timer_callback()`:
    - constructs an OccupancyGrid.
    - fills header and basic metadata.
    - fills a fixed 3x3 array of occupancy values.
    - publishes.

Functions:
- `main(args=None)`
  - init, spin node, shutdown.

Notes:
- This is not used by the main maze navigation flow.
- It is a learning/demo node for understanding OccupancyGrid.

---

## 9) Config files (maps, RViz, Nav2 params)
### 9.1 Map YAMLs
Files:
- `src/autonomous_tb3/config/maze_map.yaml`
- `src/autonomous_tb3/config/tb3_world_2d_map.yaml`

Purpose:
- A map YAML points to a `.pgm` image and defines:
  - resolution (meters per pixel)
  - origin (map frame origin in meters)
  - thresholds.

Main launch uses:
- `maze_map.yaml` (not the tb3_world_2d_map).

### 9.2 `tb3_nav_params.yaml` (Nav2 tuning)
File: `src/autonomous_tb3/config/tb3_nav_params.yaml`
Purpose:
- Contains parameters for AMCL, costmaps, planner, controller, etc.

Important notes:
- Many nodes have `use_sim_time: False` here, but the system is running in Gazebo.
  In practice you usually want `use_sim_time: True` for all Nav2 nodes.
  (In this chat we ensured the solver uses sim time; but the YAML is still mixed.)

Behavior-impacting parameters:
- Controller max velocities are set unusually high (e.g. `max_vel_x: 3.0`).

### 9.3 RViz config
File: `src/autonomous_tb3/config/tb3_nav.rviz`
Purpose:
- Preconfigured RViz view for Nav2.

### 9.4 Cartographer config
File: `src/autonomous_tb3/config/turtlebot3_cartographer.lua`
Purpose:
- Configures Cartographer for 2D mapping.
- Not used unless you launch mapping.

---

## 10) Gazebo world model (maze)
Files:
- `src/autonomous_tb3/worlds/tb3_maze_world/model.config`
- `src/autonomous_tb3/worlds/tb3_maze_world/model.sdf`

Purpose:
- Defines the maze as a Gazebo model made of many wall links (boxes).

How it is used:
- `tb3_maze_navigation.launch.py` spawns it by calling our `entity_spawner.py` node with the path to `model.sdf`.

---

## 11) Troubleshooting (fast answers)
### 11.1 “colcon build shows 0 packages finished”
Install ROS discovery plugin:
- `sudo apt-get install python3-colcon-ros`

### 11.2 “Package autonomous_tb3 not found”
- You didn’t source the right setup file.
- Correct:
  - `source /opt/ros/humble/setup.bash`
  - `source /home/prateek/Maze/install/setup.bash`

### 11.3 “executable entity_spawner.py not found”
- Ensure scripts are executable:
  - `chmod +x src/autonomous_tb3/script/*.py`
  - rebuild.

### 11.4 “Entity waffle already exists”
- Kill old Gazebo processes:
  - `pkill -f gzserver; pkill -f gzclient`

### 11.5 “AMCL cannot publish pose… Please set initial pose”
- Ensure `maze_solver.py` is running while launch is alive.
- Ensure Nav2/AMCL uses sim time.
- Ensure initial pose message is valid (normalized quaternion).

---

## 12) Questions you should now be able to answer
Plain English:
- What is Gazebo doing?
- What is RViz showing?
- What is Nav2 responsible for?
- What does the solver script do?

Technical:
- Which launch file starts which nodes?
- Which topics are important for motion?
- What is `/cmd_vel` and who publishes it?
- What is AMCL and why do we need an initial pose?
- How does the maze get into Gazebo?

---

## 13) Known rough edges / improvement ideas (not implemented here)
- `tb3_nav_params.yaml` has mixed `use_sim_time` values; ideally unify them to True for simulation.
- `package.xml` is missing some dependencies that scripts use (gazebo_msgs, nav2_simple_commander, geometry_msgs, nav_msgs). It works on full desktop installs but is not “strictly correct”.
- The controller max velocities are unusually high; could cause unrealistic motion.

(These are documentation notes only; this document does not change the project behavior.)
